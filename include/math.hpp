#ifndef MATH_HPP
/*
class running_average_t
{
public:
	void init(void* vector_, size_t size_);
	void add_new_val(auto& new_input);
private:
	void* vector;
	size_t size;
};
//shotcuts for averaging filter:
void __vec_incr(double in, double* vec, size_t size)
{
	for (int i = size; i > 0; i--)
	{
		vec[i] = vec[i - 1];
	}
	vec[0] = in;
}

double __gen_running_avg(double* vec, size_t size)
{
	double out = 0;
	for (int i = 0; i < size; i++)
	{
		out += vec[i];
	}
	return out / size;
}
*/

#endif // !MATH_HPP
